# Scaffold only: not a buildable Dockerfile yet.
# Add the verified Python runtime, dependencies, source and skill assets
# when implementing the backend. Never COPY personal Hermes state or secrets.
# Cloud Run must listen on 0.0.0.0 and its injected HTTP PORT.
