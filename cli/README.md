# CLI scaffold

CLI 제출로 결정되면 본인이 클라이언트 입력·확인·결과 출력·로컬 기록·패키징을 담당한다. 공통 서버는 친구가 담당한다.

현재 CLI 구현 언어·패키징·실행 명령은 미확정이다. 월요일 멘토링 전 앱과 CLI를 동시에 구현하지 않는다.

계산 전 조건 확인과 새 계획 적용 전 사용자 선택은 앱과 동일하게 유지한다. 원격 서버의 비밀키를 CLI에 배포하지 않는다.
