# 지금 — 이동해야 할 순간을 알려주는 AI 서비스

## 1. 이 문서의 목적

이 문서는 Hermes에서 Upstage Solar Pro4를 개발 모델로 사용해, 2명이 4일 안에 제출 가능한 MVP를 구현하기 위한 제품 정의다. 서비스 앱으로 배포할지 CLI 형태로 제출할지는 월요일 멘토링에서 결정한다.

- 서비스명: 지금(가칭)
- 제품 형태: 모바일 앱 또는 CLI. 월요일 멘토링 후 확정하며, 두 형태를 모두 제출하는 것을 기본 범위로 잡지 않는다.
- 대상 지역: 서울. 실제 지원 경로·노선은 확보한 교통 데이터 범위로 제한한다.
- 개발 인원과 기간: 2명, 개발 착수 기준 4일
- 개발 환경: Hermes + Upstage Solar Pro4
- 현재 자산: 서비스 PRD, Agent/Skill 제안서, 구현한 Timely Skill과 일부 대화 테스트 결과, 서울시 공공데이터 포털·공공데이터 포털의 발급 완료된 관련 API 키
- 공통 목표 산출물: MainAgent·SubAgent·Skill 기반 실행 흐름, 동작하는 백엔드, 재현 가능한 데모, 실행·제출 안내
- 형태별 산출물: 앱 선택 시 설치 가능한 Android APK, CLI 선택 시 다른 개발 환경에서도 실행 가능한 CLI와 설치 안내

Hermes의 특정 명령어나 프로젝트 형식은 이 문서에서 가정하지 않는다. 서비스 실행 중 사용할 AI 모델과 호출 경로는 개발 모델 설정과 구분해 결정한다. Timely 외부 호출 지원 여부, 런타임 Solar API 접근 권한, 대회 필수 사용 조건을 확인 없이 전제하지 않는다.

이 문서는 기존 PRD의 제품 방향을 유지하면서 4일 제출용 구현 범위를 제안한다. 전체 PRD를 모두 구현한 것으로 간주하지 않는다.

## 2. 한 줄 아이디어

> 약속과 막차의 도착·출발 마감시각을 기준으로 언제 움직여야 하는지 알려주고, 이동 계획이 깨지면 다음 선택을 돕는 AI 서비스.

사용자는 “오늘 7시까지 강남역에 가야 해” 또는 “막차로 집에 가려면 언제 나가야 해?”라고 입력한다. 앱 또는 CLI는 필요한 장소와 시간을 확인한 뒤, 확인 가능한 교통 정보를 바탕으로 권장 출발시각과 계산 근거를 제공한다.

화면에서 가장 중요한 정보는 지도보다 **출발해야 하는 시각**이다.

## 3. 해결할 문제와 대상 사용자

주요 대상은 서울에서 버스·지하철·도보로 이동하는 대학생과 직장인이다.

- 약속시각은 알고 있지만 이동시간과 도착 여유를 합쳐 출발시각을 매번 계산해야 한다.
- 막차시각 하나를 알아도 집까지 이어지는 환승이 가능한지는 별도로 확인해야 한다.
- 버스나 환승을 놓치면 현재 위치에서 가능한 경로와 지각 여부를 다시 판단해야 한다.
- 실제로 필요한 정보를 얻기 위해 장소·시간·교통 조건을 여러 번 입력해야 한다.

`지금`은 자연어 입력을 이동 조건으로 정리하고, 계산된 시각과 다음 행동을 짧게 보여준다. 정상적인 이동 중에는 불필요한 안내를 반복하지 않는다.

## 4. 대표 사용자 흐름

### 온보딩

1. 목적지와 무관한 기본 선호로 약속 도착 여유시간과 이용 가능한 대중교통 수단(버스·지하철)을 설정한다. 도보는 연결 구간으로 기본 허용한다.
2. 이번 이동에서 사용자가 명시한 조건을 저장된 선호보다 우선하며, 둘 다 없으면 서비스 기본값을 확인 화면 또는 CLI 확인 프롬프트에 표시한다.
3. 앱 서비스로 확정하면 사용자 선호 등 UserData를 Supabase DB에 저장한다. CLI 제출 시에는 로컬 설정·기록으로 처리하며 사용자 DB를 필수로 두지 않는다.
4. 택시비 상한선은 MVP에서 수집·저장·계산하지 않는다. 택시 관련 기능과 함께 추후 확장 모델에서 다룬다.

아래 흐름의 화면·버튼 표현은 앱 기준이다. CLI 선택 시 동일한 입력·확인·계산·선택을 명령과 확인 프롬프트로 제공한다.

### A. 약속에 맞춰 출발하기

1. 사용자가 “오늘 오후 7시까지 강남역 2번 출구에 도착해야 해”라고 입력한다.
2. 앱이 출발 기준점, 필요한 이동 조건, 도착 여유 선호를 확인한다.
3. 사용자가 정리된 이동 조건을 확인한다.
4. 서버가 확인 가능한 경로와 시간 정보를 조회하고 권장 출발시각을 계산한다.
5. 앱이 권장 출발시각, 목표 도착시각, 경로, 추가 여유의 근거를 표시한다.
6. 사용자는 이동을 저장하고 출발·도착 상태를 버튼으로 변경한다.

### B. 막차로 귀가하기

1. 사용자가 출발지와 귀가 목적지를 지정한다.
2. 서버가 지원하는 데이터 범위에서 운행일·방향·종착역·환승 연결을 확인한다.
3. 연결 가능한 마지막 Journey와 이론상 최종 출발시각을 구한다.
4. 추가 여유를 반영한 권장 최종 출발시각을 우선 표시한다.
5. 검증할 수 없는 구간은 확인 필요 또는 지원 범위 밖으로 안내한다.

### C. 계획이 깨졌을 때 다시 판단하기

1. 사용자가 “버스를 놓쳤어” 또는 재탐색 버튼을 선택한다.
2. 앱이 현재 출발 기준점·시각과 유지할 목적지·Deadline을 확인한다.
3. 서버가 현재 조건에서 가능한 경로를 다시 조회한다.
4. 앱이 새 예상 도착시각과 기존 계획 대비 변화를 표시한다.
5. 사용자가 새 계획을 선택해 적용한다.

## 5. 4일 MVP 범위

### 우선 구현

| 기능 | 최소 구현 내용 |
|---|---|
| 자연어 이동 입력 | 장소·시간·출발/도착 의미를 구조화하고 부족한 항목만 확인 |
| 약속 출발시각 | 확인된 경로를 기준으로 목표 도착시각과 권장 출발시각 계산 |
| 시간 중심 결과 | 저장한 이동, 권장 출발시각, 남은 시간, 위험·확인 필요 상태 표시. 앱은 Home, CLI는 결과·상태 출력으로 제공 |
| 경로 상세 | 도보·대중교통 구간, 예상 도착, 계산 근거, 데이터 기준시각 표시 |
| 이동 관리 | 출발·도착·취소 수동 처리. 앱 서비스는 사용자 데이터 DB 저장, CLI는 로컬 기록. 앱의 로컬 저장은 캐시 역할로 구분 |
| 사용자 요청 재탐색 | 변경된 위치·시각으로 경로를 다시 계산하고 사용자 선택으로 적용 |
| 예외 처리 | 입력 부족, 데이터 없음, 지원 범위 밖, 서버 오류를 구분 |

### 데이터 검증 후 구현

- 막차: 첫날 검증한 지역·노선·운행일 범위에서만 제공한다. 제품의 핵심 축이므로 첫날 우선 검증한다.
- 로컬 출발 알림: 앱으로 확정하고 기본 흐름 완성 후 실기기 검증까지 가능한 경우 구현한다. 일정 수정·취소 시 기존 알림도 갱신한다. CLI 제출의 필수 기능은 아니다.
- 외부 지도 연결: 앱으로 확정한 경우 확인된 목적지와 지원되는 연결 방식으로 경로 확인을 돕는다.

### 이번 제출에서 보류

- GPS 상시 추적과 자동 출발·도착 감지
- 백그라운드 교통 재계산과 자동 경로 이탈 감지
- Calendar 자동 동기화와 여러 일정의 자동 관리
- 택시비 상한선 설정·계산 및 택시 + 대중교통 최적화
- 개인화 이동시간 학습
- 소셜 기능과 고급 계정 동기화. 단, 앱 서비스에서 UserData를 DB에 저장하기 위한 최소 사용자 식별·인증·접근 권한은 별도로 정의하고 구현한다.
- 12개 Skill 각각을 별도 서비스로 구축하는 구조

자체 지도, 자체 Turn-by-turn Navigation, 전국 교통 지원은 원래 제품의 MVP 비목표다.

## 6. 계산과 사용자 확인 원칙

### 시간 계산

- 목표 도착시각 = 약속의 도착 Deadline − 사용자의 도착 여유시간
- 권장 출발시각은 해당 시각에 유효한 경로·운행시간을 기준으로 결정한다.
- 고정 이동시간으로 계산하는 경우 목표 도착시각에서 확인된 이동시간과 추가 Safety Buffer를 뺀다. 이 계산의 가정도 표시한다.
- API 이동시간에 이미 포함된 도보·대기·환승 시간을 중복 가산하지 않는다.
- Safety Buffer는 명시된 규칙과 설정으로 계산한다. 임시 수치라면 검증된 보장값처럼 표현하지 않는다.
- 날짜와 시간대를 포함해 처리한다. 서울 기준 시간대는 Asia/Seoul이며 자정 이후 운행도 구분한다.
- 현재 시점의 ETA를 미래 시각에도 그대로 적용해 도착을 보장하지 않는다.
- 막차의 이론상 최후 시각과 여유를 적용한 권장 시각을 구분한다.

### 자연어와 장소 확인

- “오후 7시까지 도착해야 해”는 도착 Deadline 19:00으로 확정한다.
- 미리 도착하고 싶은 시간은 Deadline과 별도 항목이다. 미지정 선호 때문에 명확한 Deadline을 다시 묻지 않는다.
- “내일 8시 잠실”처럼 모호하면 필요한 확인 질문을 한 번에 최대 3개까지 한다.
- 경로 계산 요청 전에 사용자에게 NLP 해석 결과(장소·시각·도착 여유·이동수단)를 표시하고 수정·최종 확인을 받는다. 앱과 CLI에 동일하게 적용한다.
- 넓은 지역명, 학교 이름, 집이라는 표현을 특정 장소로 임의 확정하지 않는다.
- 출발지에는 가까운 역·정류장이나 건물 출입구 등 필요한 최소 정보만 요청한다. 집의 동·호수를 기본 요구하지 않는다.
- 이미 확인한 정보를 반복해서 질문하지 않는다.

### 계획 변경

- 지각이 예상되면 계산 근거와 가능한 다음 행동을 제시한다.
- 추천과 다른 경로로 이동했다고 해서 무조건 잘못된 이동으로 판단하지 않는다.
- 경로 이탈 후 새 계획 적용에는 사용자 확인이 필요하다.
- MVP 재탐색은 사용자 요청으로 시작한다. 자동 재탐색을 확장할 경우 기존 PRD의 주기당 자동 재제시 최대 3회와 추가 승인 정책을 적용한다.
- 네트워크 재시도와 사용자에게 새 Journey를 제시한 횟수는 구분한다.

## 7. AI와 코드의 역할

AI는 입력 구조화, 확인 질문, 계산 결과 설명을 담당한다. 검증된 구조화 결과를 클라이언트(앱 또는 CLI)와 서버가 주고받도록 한다.

시간 계산, 경로 유효성, 상태 변경, 횟수 제한, 알림 조건은 테스트 가능한 코드로 처리한다. 모델이 교통편·요금·막차시각·이동시간을 임의로 생성하도록 두지 않는다.

AI 출력이 형식에 맞지 않거나 필요한 정보가 없으면 확인 필요 또는 오류로 처리한다. AI가 실제로 실행되지 않은 조회·저장·알림 발송을 완료했다고 설명하지 않도록 한다.

### MainAgent·SubAgent·Skill 실행 구조

- 이 구조는 개발자를 보조하는 코딩 에이전트 분업이 아니라, 제출할 서비스의 실행 구조다.
- MainAgent는 사용자 요청과 현재 상태를 파악해 적절한 SubAgent에 작업을 배정하고 실행 순서·결과 취합·사용자 확인 요청을 조정한다.
- SubAgent에는 우리가 구현한 Skill을 역할에 맞게 할당한다. SubAgent는 해당 작업에 필요한 Skill을 선택해 사용한다.
- SubAgent와 Skill은 1:1 대응이 아니다. 하나의 SubAgent가 여러 Skill을 사용할 수 있으며, 모든 Skill을 매 요청마다 실행하지 않는다.
- 실제 SubAgent 목록과 Skill 묶음은 구현 계획에서 정한다. 8절의 핵심·선택·후속 우선도를 유지하며, 기존 Skill이 있다는 이유만으로 보류 기능을 활성화하지 않는다.
- 경로 조회, 시간 계산, 상태 변경, 권한 검증 등 코드로 처리하는 것이 적합한 기능은 도구·도메인 로직으로 구현한다. 무리하게 별도 Agent를 할당하지 않는다.
- MainAgent·SubAgent 사이에는 검증 가능한 구조화 입력·출력을 사용한다. 구현 시 호출 횟수·전체 제한시간·실패 시 반환 규칙을 정하고 API 계약의 제한시간과 일치시킨다.

## 8. 기존 Skill 자산의 활용

기존에 구현한 Timely Skill을 확보해 SubAgent가 사용할 실행 자산으로 통합한다. Skill 생성 지침과 테스트 대화는 구현 파일·검증 근거와 구분한다. Timely에서 동작한 Skill이 새 런타임에서도 그대로 동작한다고 가정하지 않으며, 필요한 도구·입출력·권한을 연결하고 재검증한다. 이 자산이 있다는 사실만으로 앱 API나 보류 기능까지 구현됐다고 간주하지 않는다.

| Skill | 앱에서 활용할 책임 | 이번 우선도 |
|---|---|---|
| resolve-trip-details | 이동 조건 해석·확정 | 핵심 |
| leave-by | 약속 출발시각 정책·설명 | 핵심 |
| maginot-line | 막차 마지노선 정책·설명 | 데이터 검증 우선 |
| plan-b-recovery | 재탐색 대안 비교·설명 | 핵심 |
| route-risk-checker | 계산된 여유·위험의 설명 | 핵심 결과에 통합 |
| journey-check-in | 사용자의 출발·도착 확인 해석 | 수동 상태 흐름에 필요한 부분만 |
| departure-alert | 출발 알림 판단 규칙 | 선택 구현 |
| schedule-ready | 일정 준비 정보 확인 | 입력 흐름에 필요한 부분만 |
| calendar-conflict | 여러 일정의 연결 가능성 | 후속 확장 |
| taxi-hybrid | 최소 택시 구간 대안 | 후속 확장 |
| route-deviation | 이탈 후 재탐색 승인 | 수동 흐름의 확인 정책 참고 |
| replan-guard | 재탐색 주기·횟수 정책 | 자동 재탐색 도입 시 적용 |

## 9. 클라이언트와 시스템 구조

앱 선택 시 화면은 Home, 이동 입력·조건 확인, 이동 상세, 재탐색 결과를 중심으로 구성한다. 상태별 안내는 해당 화면에 표시하고 불필요한 별도 화면을 늘리지 않는다. CLI 선택 시 동일한 흐름을 텍스트 입력·조건 확인·계획 출력·수동 상태 변경·재탐색으로 제공한다.

```text
클라이언트: 모바일 앱 또는 CLI (멘토링 후 하나 선택)
  입력 · 확인 · 결과 · 수동 이동 상태
       ↓
FastAPI 백엔드 (Docker 패키징 → Cloud Run 실행·배포)
  입력 검증 · MainAgent의 작업 배정·결과 취합
       ├─ SubAgent들 → 역할별 Skill 선택·사용 (한 Agent에 여러 Skill 가능)
       ├─ 코드 도구 → 시간 계산·상태 처리·공공데이터 API 연결
       └─ 앱 서비스 확정 시 → Supabase PostgreSQL: UserData 저장
```

클라이언트와 서버는 입력 해석, 계획 계산, 재탐색의 요청·응답 계약을 먼저 공유한다. 공통 결과는 성공, 추가 확인 필요, 데이터/경로 이용 불가, 오류로 구분한다. 결과에는 출처와 데이터 기준시각을 포함한다.

### 확정된 기술과 조건부 선택

- 백엔드: FastAPI.
- 컨테이너 패키징: Docker.
- 백엔드 실행·배포 환경: GCP Cloud Run.
- 앱 선택 시: React Native + Expo로 개발하고 스토어 등록 없이 Android APK로 배포한다. 빌드·서명·설치 절차를 첫날 검증한다.
- CLI 선택 시: 공통 백엔드를 사용하는 CLI를 제출한다. CLI 구현 언어·패키징은 멘토링 후 확정한다.
- 앱 서비스 선택 시 DB: Supabase PostgreSQL에 UserData를 저장한다. 저장 항목·사용자 식별·인증·사용자별 접근 제어·삭제 정책과 API를 구현 전에 합의한다. 앱 로컬 캐시와 DB 중 데이터의 기준을 명확히 하고, 인증 정보 없이 사용자가 보낸 user_id만으로 접근을 허용하지 않는다.
- 교통 데이터: 서울시 공공데이터 포털과 공공데이터 포털의 API를 호출해 실시간 데이터를 받는다. 관련 API 키는 모두 발급받은 상태다. 키 발급 완료와 실제 응답·지원 범위 검증 완료는 구분한다.
- 미확정: 실행용 AI 모델의 정확한 호출 설정, 사용할 개별 공공데이터 API의 서비스명·엔드포인트·조회 조합. 값을 추측하지 않는다.

실시간 도착 정보만으로 출발지부터 목적지까지의 전체 경로, 미래 시각 경로, 막차 환승 연결이 제공된다고 가정하지 않는다. 확보한 API별 실시간 정보·노선·정류장·시간표 제공 범위를 확인하고, 부족한 경로 연결 로직과 장소 검색 방법을 첫날 검증한다.

### 앱 서비스 확정 시 DB 연결과 포트

- 앱 서비스로 확정하면 백엔드에서 PostgreSQL 전용 TCP 5432 포트로 DB에 연결할 수 있도록 필요한 네트워크 접근을 구성한다. 실제 Supabase 연결 주소·모드·TLS 설정은 대시보드와 실행 환경을 확인해 결정한다.
- 5432는 백엔드에서 DB로 연결하는 포트이며, Cloud Run에 외부 공개할 HTTP 포트가 아니다. Cloud Run의 FastAPI 컨테이너는 플랫폼이 제공하는 PORT 환경변수와 0.0.0.0으로 HTTP 요청을 받는다.
- 앱·CLI는 HTTPS API를 호출하며 DB에 직접 접속하지 않는다. DB 포트를 인터넷 전체에 무제한 공개하거나 DB 비밀번호를 클라이언트에 포함하지 않는다.
- Supabase의 direct 연결과 session pooler는 5432, transaction pooler는 6543을 사용하므로 서로 혼동하지 않는다. 5432 연결의 IP 호환성·연결 수를 검증하고, 다른 모드가 필요하면 두 개발자가 합의해 문서를 갱신한다.

외부 API 키와 DB 자격증명은 서버 환경에서 관리한다. 클라이언트에는 서버가 반환한 결과를 전달한다. 문서·로그·앱 번들에 실제 키 값을 기록하지 않는다.

## 10. 두 사람의 분업과 일정

| 담당 | 책임 |
|---|---|
| 본인 | 앱 화면, 입력 확인 UI, 서버 연결, 로컬 저장, 수동 상태, 알림 선택 구현, 기기 빌드 |
| 친구 | 서버, MainAgent·SubAgent·Skill 통합, AI·교통 데이터 API 연결, 시간 계산, 재탐색, 서버 배포, 앱 서비스 선택 시 UserData DB·인증·권한 |
| 공동 | API 계약, 통합 테스트, 지원 범위 결정, 데모·제출 자료 |

본인은 앱, 친구는 서버를 담당하는 것으로 확정한다. CLI 제출로 바뀌면 본인이 클라이언트 입출력·확인 흐름·패키징을 맡고 친구는 공통 서버·Agent·Skill·데이터 연동을 유지한다. 앱 서비스로 확정하면 친구의 서버 책임에 Supabase UserData 저장과 인증·권한 처리를 포함한다. 두 개발자 모두 Spec Kit을 활용한다.

월요일 멘토링에서 앱 서비스 또는 CLI 제출을 결정한다. 결정 전에는 공통 Agent·Skill 실행 구조, API 계약, 공공데이터 검증을 우선하고 앱·CLI를 동시에 완성하는 범위로 늘리지 않는다. 아래 일정은 개발 착수 기준이며, 형태 결정 후 남은 기간에 맞춰 조정한다.

| 날짜 | 목표 |
|---|---|
| Day 1 | 첫 2시간에 입출력 계약 합의. 첫 4~6시간에 발급된 키로 교통 API·막차 지원 검증. 배포 서버 호출 성공. 형태 확정 시 앱 설치 또는 CLI 실행 확인. 앱 서비스라면 UserData·인증 계약도 합의 |
| Day 2 | 자연어 → 조건 확인 → 실제 데이터 기반 출발시각 표시를 완성. 선택한 막차 범위 구현 |
| Day 3 | 재탐색·오류 처리·시간 경계 검증. 저녁부터 기능 추가 중단 |
| Day 4 | 앱은 다른 기기에 설치, CLI는 다른 환경에 설치해 최종 동작 확인. 치명적 오류 수정, 영상·설명 작성, 제출 |

매일 최소 두 번 통합한다. 샘플 응답으로 클라이언트를 개발할 수 있지만 실제 데이터 경로와 구분한다. 마지막 날 처음 앱 빌드·CLI 패키징·서버 배포를 시도하지 않는다.

## 11. 멘토링 결정과 첫날 검증 항목

- 월요일 멘토링: 서비스 앱 배포 또는 CLI 제출을 결정하고 실제 제출 조건과 대조한다. 앱 선택 시 스토어 없이 APK 배포
- 개발 모델은 Hermes의 Solar Pro4로 확정. 제공된 환경에서 설정·호출을 확인
- 서비스 실행 중 필요한 모델 호출 방식 및 Timely/Solar 관련 필수 조건
- 발급 완료된 서울시 공공데이터 포털·공공데이터 포털 API 키로 실제 응답, 권한, 이용 조건, 호출 한도, 갱신 주기를 확인
- 개별 API 서비스명·엔드포인트·지원 노선 목록, 장소 검색·경로 연결 방법
- 미래 시각 기준 경로 계산과 막차·환승 검증이 가능한 데이터 범위
- SubAgent별 Skill 할당, 도구 연결, 모델 호출 횟수·제한시간·실패 정책
- 앱 서비스 선택 시 UserData 저장 항목·인증·권한·삭제 계약 및 PostgreSQL 5432 연결 검증. API_SPEC.md의 기존 로컬 전용 계약을 구현 전에 개정

막차 데이터가 일부만 가능하면 해당 노선·조건으로 범위를 제한한다. 확보할 수 없다면 사용자 제공 시간표 기반 계산으로 제한하거나 제출 범위에서 보류하는 결정을 첫날 팀에 제시한다. 실제 데이터가 없는 상태를 LLM 추정으로 채우지 않는다.

## 12. 완료 기준

완료 여부는 대화 응답이나 화면 시안이 아니라 아래 동작으로 판단한다.

1. 선택한 제출 형태에 따라 다른 실제 기기에 앱을 설치하거나 다른 개발 환경에서 CLI를 실행해 배포된 서버에 연결할 수 있다.
2. 자연어 요청에서 모호한 장소·시간을 확인하고 명확한 조건은 유지한다.
3. 확인된 이동 데이터로 권장 출발시각과 계산 근거를 표시한다.
4. 이동을 저장하고 클라이언트를 다시 실행해 확인할 수 있다. 앱 서비스는 Supabase UserData 저장·사용자별 접근 제어를 검증하고, CLI는 로컬 기록을 검증한다.
5. 사용자 요청으로 재탐색하고 새 계획을 선택할 수 있다.
6. 선택한 막차 지원 범위에서 운행일·종착역·환승 연결을 검증할 수 있다. 미지원 상태는 명시한다.
7. 데이터 없음과 API 오류에서 거짓 경로 대신 다음 행동을 제공한다.
8. 도착 여유 중복 계산, 자정 경계, 사용자 고정 조건을 검증한다.
9. 앱에서 알림을 구현했다면 권한 거부와 일정 수정·취소 후 알림 동작을 실기기에서 확인한다. CLI 제출에는 이 기준을 적용하지 않는다.
10. 설치 방법, 서버 실행·배포 방법, 필수 설정, 지원 범위, 알려진 제한을 문서화한다.
11. MainAgent가 적절한 SubAgent를 지휘하고 SubAgent가 할당된 Skill을 실제 사용함을 테스트로 확인한다. 단일 SubAgent가 여러 Skill을 사용하는 흐름을 지원하며 코드 계산과 사용자 확인 원칙을 유지한다.

데모 데이터로 재현한 장면은 그 사실을 표시한다. 관측하지 않은 실시간 교통 변화나 자동 모니터링을 구현 완료로 소개하지 않는다.

## 13. Hermes에서 작업을 시작할 때

먼저 저장소와 제공된 문서를 읽고 이미 구현된 부분이 있는지 확인한다. 이 문서의 확정된 제품 방향을 유지하면서 미확정 기술 선택은 근거를 남겨 구체화한다.

첫 작업 결과로 클라이언트·서버 책임, MainAgent·SubAgent·Skill 할당 계획, 최소 API 계약, 데이터 검증 결과, 4일 실행 순서를 정리한다. 멘토링 전에는 앱 배포를 확정된 것으로 취급하지 않는다. 선택한 클라이언트와 서버를 처음부터 함께 연결하며 가장 작은 약속 이동 흐름을 완성한다. 핵심 흐름이 동작하면 막차와 재탐색을 지원 범위 안에서 확장한다.

핵심 기준은 사용자가 자신의 이동 조건에 맞는 **출발시각과 다음 행동을 이해하고 실제로 사용할 수 있는가**다.

두 개발자 모두 [Spec Kit](https://github.com/github/spec-kit)을 활용해 담당 기능의 명세·계획·작업을 관리한다. IDEA.md는 제품 범위와 확정 결정의 기준, API_SPEC.md는 클라이언트·서버 요청·응답 계약의 기준으로 사용한다. 계약 변경은 공동 합의 후 기준 문서와 예제 JSON에 먼저 반영하며, 각자 생성한 명세가 서로 다른 API나 범위를 정의하지 않도록 한다. 현재 API_SPEC.md는 로컬 저장 중심 초안이므로 앱 서비스의 UserData 저장·인증 계약이 확정되기 전까지 이를 완료된 서비스 계약으로 간주하지 않는다.

## 참고 자료

이 문서는 단독으로 읽어도 개발 목적과 범위를 이해할 수 있도록 작성했다. 아래 파일이 Hermes 작업 공간에도 있다면 세부 맥락을 함께 확인한다.

- `Docs/jigeum_prd_v1.0.md`: 전체 서비스 PRD
- `API_SPEC.md`: 공통 계산 API 초안. 앱 서비스의 UserData·인증 계약은 형태 확정 후 추가 필요
- `Docs/jigeum_agent_skills_proposal_v0.1.md`: Agent와 코드 기반 도메인 로직의 책임
- `Docs/jigeum_4day_mvp_team_plan_v0.1.md`: 2인·4일 분업 제안
- `Docs/timely_skill_submission_plan_v0.1.md`: 최초 6개 Skill 계획
- `Docs/timely_additional_skills_my_prompts_v0.1.md`: 본인 담당 추가 Skill 생성 지침
- `Docs/timely_additional_skills_friend_prompts_v0.1.md`: 친구 담당 추가 Skill 생성 지침

기술 확인 자료:

- [Cloud Run 컨테이너 실행 조건](https://docs.cloud.google.com/run/docs/container-contract): HTTP 리스닝 주소·PORT와 컨테이너 요구 사항
- [Supabase DB 연결 가이드](https://supabase.com/docs/guides/database/connecting-to-postgres): 5432/6543 연결 모드·IP 호환성·연결 풀·TLS 설정
