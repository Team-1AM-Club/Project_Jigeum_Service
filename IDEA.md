# 지금 — Hermes에서 사용하는 이동 판단 MCP 서비스

## 1. 문서의 목적과 확정 방향

이 문서는 2명이 개발 착수 기준 4일 안에 제출할 수 있는 지금(Jigeum) MVP의 제품 정의다. 개발과 시연 모두 Hermes + Upstage Solar Pro4를 사용한다. **서비스 자체를 MCP로 제공한다.** MCP는 임시 시연용 포장이나 별도 제품 개발 전 단계가 아니다. 독립 사용자 클라이언트는 제품 제공 계획에 포함하지 않는다. Hermes는 개발·시연에 사용할 MCP 클라이언트다.

- 방향 갱신일: 2026-09-14
- 서비스명: 지금(가칭)
- 제품 제공 형태: MCP 서비스로 확정. 지금 MCP 서버가 외부 기능 인터페이스이며, FastAPI는 이를 지원하는 내부 계산 백엔드다.
- 사용 방식: MCP 클라이언트에서 지금의 도구를 호출한다. 첫 검증·시연 대상은 Hermes이며, 다른 클라이언트의 호환성은 별도 검증 전까지 보장하지 않는다.
- 우선 연결 방식: Hermes가 로컬 MCP 서버를 실행하는 stdio
- MCP 서버의 역할: 도구를 노출하고 기존 FastAPI API에 요청을 전달하는 얇은 연결 계층
- 담당: 본인은 MCP·Hermes 연결·사용 흐름·시연, 친구는 핵심 백엔드·Agent·교통 데이터·계산
- 대상 지역: 서울. 실제 지원 경로·노선·운행일은 검증된 데이터 범위로 제한한다.
- 현재 자산: 서비스 PRD, Agent/Skill 제안서, Timely Skill과 일부 대화 테스트, 기존 백엔드 코드, 발급된 서울시 공공데이터 포털·공공데이터 포털 API 키

이번 develop 공통 인수인계에는 문서·계약·폴더 골격만 포함한다. 아래의 기존 코드·Skill·API 키 등은 팀이 보유한 자산 설명이며 공유 커밋에 전부 포함됐다는 뜻이 아니다. 작성 중 코드와 Skill ZIP은 로컬에 남기고, 비밀키는 커밋하지 않는다.

이 문서는 제품 방향과 목표이며 구현 완료 보고서가 아니다. 기존에 확인한 백엔드 라우터는 mock provider와 meta.is_demo=true를 사용한다. 코드·API 키·Skill 파일이 있다는 사실만으로 실제 교통 조회, MCP 연결, 모델 호출, 배포가 완료됐다고 간주하지 않는다.

Hermes 공식 문서는 로컬 stdio와 원격 HTTP MCP 연결을 지원한다고 설명한다. 다만 팀에 설치된 Hermes 버전과 Solar Pro4의 실제 도구 호출은 첫날 검증한다. 모델의 정확한 ID·제공처·API 접근 권한은 확인해 설정하고 추측하지 않는다.

주최 측이 원격 MCP 접속 주소나 별도 배포 형태를 요구하는지는 확인한다. 로컬 시연 성공을 대회 제출 조건 충족으로 간주하지 않는다.

## 2. 한 줄 아이디어와 해결할 문제

> 약속·막차에 맞춘 출발시각과 계획 실패 시 대안을 MCP 도구로 제공하는 AI 이동 판단 서비스.

사용자는 Hermes에 “오늘 7시까지 강남역에 가야 해” 또는 “막차로 집에 가려면 언제 나가야 해?”라고 입력한다. Hermes는 MCP 도구를 사용해 필요한 조건을 확인하고, 서버가 검증한 교통 정보와 계산 결과를 설명한다.

주요 대상은 서울에서 버스·지하철·도보로 이동하는 대학생과 직장인이다.

- 약속시각에 이동시간과 도착 여유를 맞춰 출발시각을 매번 계산해야 한다.
- 막차시각 하나를 알아도 집까지 이어지는 환승이 가능한지 별도로 확인해야 한다.
- 버스나 환승을 놓치면 현재 위치에서 가능한 경로와 지각 여부를 다시 판단해야 한다.
- 장소·시간·교통 조건을 반복 입력하거나 이미 명확한 내용을 다시 설명해야 한다.

결과에서 가장 중요한 정보는 지도보다 **출발해야 하는 시각과 지금 할 행동**이다. 정상적인 이동 중에는 불필요한 안내를 반복하지 않는다.

## 3. 사용자 인터페이스와 시스템 구조

MCP는 화면이나 AI 모델이 아니라 AI 클라이언트와 외부 도구를 연결하는 규약이다. Hermes가 대화와 도구 호출을 맡고, 지금 MCP 서버와 백엔드가 서비스 기능을 제공한다.

~~~text
사용자
  ↕ 자연어 입력 · 질문 응답 · 조건 확인 · 후보 선택
Hermes + Upstage Solar Pro4
  ↕ MCP 도구 호출 / 구조화 결과 — 로컬 stdio
지금 MCP 서버                                      ← 본인
  도구 스키마 검증 · HTTP 연결 · 결과/오류 전달
  ↕ 기존 REST/JSON API
FastAPI 백엔드                                     ← 친구
  입력 검증 · MainAgent의 작업 배정/결과 취합
    ├─ SubAgent → 역할별 Skill 선택/사용
    └─ 코드 도구 → 교통 조회 · 경로 검증 · 시간 계산
  ↕
검증된 장소·교통 데이터 제공처
~~~

### 재사용 원칙과 실행 환경

- 기존 FastAPI API와 도메인 로직을 유지하고 MCP 연결 계층을 추가한다. 백엔드 전체를 MCP 전용으로 다시 작성하지 않는다.
- Hermes는 외부 대화·도구 호출 호스트다. 기존 서비스 MainAgent를 자동 대체하지 않는다.
- 개발용 Hermes 세션과 시연용 Hermes 세션은 분리한다. 시연은 MCP 도구 호출로 수행하며 개발용 파일 편집·터미널 작업을 서비스 기능처럼 보여주지 않는다.
- 백엔드 기술은 FastAPI, 패키징은 Docker, 배포 대상은 기존 결정인 GCP Cloud Run을 유지한다.
- 초기 연결 점검에는 로컬 FastAPI를 사용할 수 있다. 배포 여부와 최종 시연 Base URL은 실제 실행 결과로 기록한다.
- 로컬 stdio MCP 서버와 Cloud Run의 HTTP 백엔드는 서로 다른 실행 경계다. Cloud Run에서 로컬 stdio를 직접 제공한다고 설명하지 않는다.
- 원격 MCP가 제출 조건상 필요하면 연결 방식·인증·배포 범위를 별도 합의한다. 원격 MCP 공개는 기본 로컬 stdio MVP에 자동 포함하지 않는다.

### 데이터·모델·보안

- 교통 데이터는 서울시 공공데이터 포털과 공공데이터 포털의 발급된 키로 조회한다. 사용할 개별 API의 서비스명·엔드포인트·권한·응답·지원 범위는 실제 검증한다.
- 실시간 도착 정보만으로 전체 경로, 미래 시각의 경로, 막차 환승 연결이 제공된다고 가정하지 않는다. 부족한 경로 연결 로직과 장소 검색 방법을 첫날 확인한다.
- Hermes의 개발·시연 모델은 Solar Pro4다. 백엔드 내부 Agent의 런타임 모델 ID·호출 경로·접근 권한은 별도 확인한다. Hermes 설정만으로 백엔드 AI까지 연결됐다고 보지 않는다.
- 별도 회원가입·Supabase UserData DB는 이번 MCP MVP의 필수가 아니다.
- 교통·백엔드 AI 비밀키는 백엔드 환경에서 관리한다. Hermes 모델 자격증명은 Hermes 실행 환경의 비밀 설정으로 관리한다.
- 실제 키를 저장소·설정 예제·로그·시연 영상에 넣지 않는다. MCP 연결 설정에는 필요한 최소 권한만 부여한다.
- 공개 백엔드는 로컬 MCP 시연과 별개로 접근 제어·호출 제한을 검토한다. 향후 DB를 추가할 때 사용자 식별·권한·삭제 계약을 먼저 합의하고 클라이언트에서 DB에 직접 접속하지 않는다.

## 4. MCP 도구 범위와 기존 API 연결

Skill 12개를 MCP 도구 12개로 일대일 변환하지 않는다. 외부 도구는 사용자 작업 단위로 묶고, 내부 Agent가 필요한 Skill을 선택한다.

아래 도구 이름과 연결은 이번 MVP의 설계 기준이다. 세부 입력·출력 JSON Schema와 MCP 오류 매핑은 두 개발자가 구현 전에 합의한다. 아래 표만으로 MCP 계약이 구현됐다고 간주하지 않는다.

| MCP 도구 | 사용자 작업 | 기존 FastAPI 연결 |
|---|---|---|
| get_capabilities | 지원 교통수단·지역·운행일·기본값·데이터 상태 확인 | GET /api/v1/capabilities |
| search_places | 출발지·목적지 후보 검색 | GET /api/v1/places |
| interpret_trip | 이동 조건 구조화, 부족한 정보와 확인 질문 반환 | POST /api/v1/mobility/interpret |
| plan_journey | 확인된 조건으로 약속 출발시각 또는 막차 계획 계산 | POST /api/v1/journeys/plan |
| replan_journey | 사용자 요청에 따라 대안과 기존 계획 대비 변화 계산 | POST /api/v1/journeys/replan |

- plan_journey의 약속·막차 구분은 기존 trip.kind 계약을 따른다.
- GET /api/v1/health는 연결 점검용으로 유지하며, 별도 사용자용 MCP 도구로 노출하는 것은 필수가 아니다.
- MCP 계층은 기존 status, data, error, meta와 출처·기준시각·데모 여부를 손실 없이 전달한다.
- 성공, 추가 확인 필요, 지원 범위·경로 이용 불가, 오류를 구분한다. HTTP 200이라는 이유만으로 계산 성공으로 설명하지 않는다.
- MCP 프로토콜·도구 실행 오류와 백엔드의 업무상 unavailable 응답을 구분해 매핑한다.
- 서버가 반환하지 않은 경로·시각·저장 결과를 MCP 계층이나 Hermes가 만들어 보충하지 않는다.

### API 계약과 문서의 관계

API_SPEC.md는 MCP 연결 계층이 호출하는 기존 REST/JSON 계산 계약의 기준이다. 서비스 제공 형태는 MCP로 확정하며 과거 독립 클라이언트 제안은 개발 지시가 아니다. 제품 방향 변경만으로 API의 필드·상태·오류를 임의로 바꾸지 않는다.

제품 형태·담당·사용 흐름의 설명은 관련 개발 문서와 일치시킨다. 이번 방향 정정은 REST 요청·응답 스키마와 예제 JSON의 변경을 의미하지 않는다. 두 개발자가 MCP 스키마, 확인·선택 상태의 소유권, 시간 초과·오류 매핑을 합의하고 관련 명세에 반영한다. REST 계약 변경이 필요하면 API_SPEC.md와 Docs/api/examples.json을 함께 갱신한다.

## 5. 대표 사용자 흐름

### 기본 선호와 입력

1. 이번 이동에서 명시한 도착 여유와 이동수단을 우선한다. 버스·지하철을 대상으로 하며 도보는 연결 구간으로 기본 허용한다.
2. 확인된 선호가 없으면 capabilities의 서비스 기본값을 최종 조건 요약에 표시한다.
3. Hermes가 집 주소나 개인 선호를 저장하고 있다고 임의 가정하지 않는다.
4. 택시비 상한선은 수집·저장·계산하지 않는다. 집의 동·호수 같은 불필요한 상세 개인정보를 요구하지 않는다.

### A. 약속에 맞춰 출발하기

1. 사용자가 “오늘 오후 7시까지 강남역 2번 출구에 도착해야 해”라고 입력한다.
2. Hermes가 interpret_trip과 search_places를 사용해 출발 기준점·장소 후보·필요한 이동 조건을 확인한다.
3. 해석된 장소·날짜·Deadline·도착 여유·이동수단을 요약하고 사용자의 최종 확인을 받는다.
4. plan_journey가 서버를 호출하고, 서버가 확인 가능한 경로·시간 정보를 조회해 계산한다.
5. Hermes가 권장 출발시각, 목표 도착시각, 경로, 추가 여유의 근거, 출처와 기준시각을 표시한다.
6. 사용자가 후보를 선택한다. 현재 대화의 선택 계획으로 관리하며 영구 저장·자동 알림을 완료했다고 말하지 않는다.

### B. 막차로 귀가하기

1. 사용자가 출발지와 귀가 목적지를 지정한다.
2. 운행일·장소·이동 조건을 요약해 사용자 확인을 받고 plan_journey를 막차 유형으로 호출한다.
3. 서버는 검증된 범위에서 방향·종착역·시간표·환승 연결을 확인해 연결 가능한 마지막 Journey를 구한다.
4. 이론상 최종 출발시각과 여유를 반영한 권장 최종 출발시각을 구분해 표시한다.
5. 검증할 수 없는 구간·날짜는 확인 필요 또는 지원 범위 밖으로 안내한다.

### C. 계획이 깨졌을 때 다시 판단하기

1. 사용자가 “버스를 놓쳤어. 다시 찾아줘”라고 요청한다.
2. Hermes가 현재 출발 기준점과 유지할 목적지·Deadline을 확인한다.
3. replan_journey가 기존 선택 계획의 비교 정보와 확인된 조건을 전달한다. 실제 재계산은 서버의 현재 시각을 기준으로 한다.
4. Hermes가 새 예상 도착시각과 기존 계획 대비 변화를 표시한다.
5. 사용자가 새 후보를 선택한 뒤 현재 대화의 선택 계획을 갱신한다. 조회 성공만으로 기존 계획을 자동 교체하지 않는다.

## 6. 4일 MVP 범위

### 우선 구현

| 기능 | 최소 구현 내용 |
|---|---|
| MCP 연결 | Hermes의 도구 발견·호출, FastAPI 연결, 구조화 결과·오류 전달 |
| 자연어 이동 입력 | 장소·시간·출발/도착 의미 구조화, 부족한 항목만 확인 |
| 조건 확인 | 계산 전 해석 결과 표시, 사용자 최종 확인, 변경 조건 재확인 |
| 약속 출발시각 | 확인된 경로로 목표 도착시각·권장 출발시각 계산 |
| 시간 중심 결과 | 권장 출발시각·예상 도착·위험·확인 필요 상태·다음 행동 표시 |
| 경로 상세 | 도보·대중교통 구간, 추가 여유 근거, 출처·데이터 기준시각 표시 |
| 계획 선택·문맥 | 현재 이동 조건과 선택 후보를 관리하고 재탐색에 전달 |
| 사용자 요청 재탐색 | 변경 조건으로 계산하고 사용자 선택 후 적용 |
| 예외 처리 | 입력 부족, 데이터 없음, 지원 범위 밖, 서버 오류·시간 초과 구분 |

### 첫날 데이터 검증 후 구현

- 막차는 제품의 핵심 축이므로 첫날 우선 검증한다. 제공 가능한 지역·노선·운행일·환승 범위에서만 구현한다.
- 데이터가 일부만 가능하면 해당 조건으로 지원 범위를 제한한다.
- 데이터를 확보할 수 없다면 사용자 제공 시간표 기반의 조건부 계산 또는 제출 기능 보류를 팀과 결정한다. 실제 서비스 조회와 사용자 제공 자료·데모를 구분한다.
- 외부 지도 링크는 서버가 검증된 URL을 제공할 때 전달할 수 있다. 자체 지도 화면은 만들지 않는다.

### 제품 제공 형태에서 제외

- 독립 사용자 클라이언트, 스토어 등록·클라이언트 설치 파일 빌드는 개발 계획에 포함하지 않는다.
- 별도 제품 출시를 전제로 MCP를 임시로 제공하는 단계적 계획은 없다. 서비스 제공 방식은 MCP다.

### MCP 기능 중 이번 제출에서 보류

- 회원가입·Supabase UserData DB·영구 이동 이력·계정 동기화·소셜 기능
- 출발·도착·취소 상태의 별도 관리 도구
- 예약 출발 알림·Push·백그라운드 자동 알림
- GPS 상시 추적·자동 출발/도착 감지·자동 경로 이탈 감지
- 백그라운드 교통 재계산·자동 재탐색
- Calendar 자동 동기화·여러 일정 자동 관리
- 택시비 상한선·택시와 대중교통 최적화
- 개인화 이동시간 학습
- 12개 Skill 각각을 별도 서비스 또는 MCP 도구로 구축하는 구조
- 자체 지도·Turn-by-turn Navigation·전국 교통 지원

Hermes나 MCP에 자동화 기능이 있더라도 이번 제품 범위를 자동 확장하지 않는다.

## 7. 계산·사용자 확인·상태 관리 원칙

### 시간과 경로

- 목표 도착시각 = 약속의 도착 Deadline − 사용자의 도착 여유시간.
- 권장 출발시각은 해당 시각에 유효한 경로·운행시간을 기준으로 결정한다.
- 고정 이동시간 계산은 목표 도착시각에서 확인된 이동시간과 추가 Safety Buffer를 빼며 그 가정을 표시한다.
- API 이동시간에 이미 포함된 도보·대기·환승·추가 여유를 중복 가산하지 않는다.
- Safety Buffer는 명시된 규칙·설정으로 계산한다. 임시 수치를 검증된 보장값처럼 표현하지 않는다.
- 날짜와 시간대를 함께 처리한다. 서울 기준 Asia/Seoul을 사용하고 자정 이후 도착과 운행일을 구분한다.
- 현재 ETA를 미래 시각에도 그대로 적용해 도착을 보장하지 않는다.
- 막차의 이론상 최후 시각과 여유를 적용한 권장 시각을 구분한다.
- 약속 시작시각 지각과 사용자가 원하는 조기 도착 여유 부족을 구분한다.
- 이동시간·조기 퇴실 불가 같은 고정 조건을 임의로 바꾸지 않는다. 조건 변경 대안은 실제 가능 여부와 사용자 동의가 필요한 선택지로 표시한다.

### 자연어와 사용자 확인

- “오후 7시까지 도착해야 해”는 도착 Deadline 19:00으로 해석한다. 도착 여유 미지정을 이유로 명확한 Deadline을 다시 묻지 않는다.
- “내일 8시 잠실”처럼 모호한 입력에는 필요한 확인 질문을 한 번에 최대 3개까지 한다.
- 넓은 지역명·학교·집을 특정 장소로 임의 확정하지 않는다. 장소 후보를 보여주고 실제 이동 기준점을 확인한다.
- 출발지에는 가까운 역·정류장·건물 출입구 등 필요한 최소 정보만 요청한다.
- 이미 확인한 정보를 반복 질문하지 않는다.
- 경로 계산 전에 장소·날짜·시각·도착 여유·이동수단을 보여주고 수정·최종 확인을 받는다.
- interpret_trip의 ready_for_plan=true는 입력 준비 완료이지 사용자 동의가 아니다.
- 모델이 user_confirmed=true를 생성했다는 사실만으로 사람의 동의가 검증됐다고 보지 않는다.
- 구현 명세에서 최종 확인한 조건과 사용자 응답을 계산 요청에 결부하는 방법을 정의한다. 미확인 호출과 조건 변경 후 과거 확인의 재사용을 막는 검증을 작성한다. 프롬프트만으로 강제가 보장된다고 주장하지 않는다.

### 대화 문맥과 선택 계획

- 기본 MVP는 현재 대화의 이동 조건과 선택 계획을 다룬다. 재시작 후 자동 복원·장기 보존은 약속하지 않는다.
- 기존 REST 서버는 요청별 계산 계약이다. plan_id만으로 서버 DB에서 과거 계획을 복원할 수 있다고 가정하지 않는다.
- 재탐색에는 기존 API가 요구하는 trip, 이전 선택 요약, 현재 출발 기준점, 확인 상태를 전달한다.
- 구현 전에 두 개발자가 문맥·확인·선택 상태를 Hermes와 MCP 계층 중 어디서 어떻게 보관할지 명세로 확정한다. 다른 대화의 상태를 섞지 않고, 모델의 자유 서술만을 신뢰해 상태를 덮어쓰지 않는다.
- 필요한 문맥을 잃으면 사용자에게 다시 확인한다. 기억하지 못한 계획을 만들어 복원하지 않는다.
- 새 후보 조회와 선택 적용은 별개다. 사용자 선택 전에는 기존 선택을 유지한다.
- 재탐색 실패·취소는 기존 기록을 삭제하지 않는다. 다만 기존 경로가 여전히 유효하다는 보장으로 표시하지 않는다.
- 선택하지 않은 후보의 비교 설명을 선택한 후보의 결과처럼 제시하지 않는다.
- 추천과 다른 경로로 이동했다고 무조건 잘못된 이동으로 판단하지 않는다.
- MVP 재탐색은 사용자 요청으로 시작한다. 기존 PRD의 자동 재제시 최대 3회 정책은 향후 자동 재탐색 확장 시 적용하며 수동 요청 횟수 제한과 혼동하지 않는다.

### 실패와 호출 제한

- 입력 부족, 제공처 미지원, 검증 범위 내 경로 없음, 제공처 장애를 구분한다.
- 실제 데이터 조회 실패를 Demo 경로로 자동 대체하지 않는다.
- 기존 API의 시간 초과·재시도 계약을 기준으로 MCP 대기 시간을 정합적으로 설정한다. 제한을 바꾸면 공동 합의한다.
- 계산 POST를 무제한 자동 재시도하지 않는다. 모델·교통 API 호출 비용과 호출 횟수·전체 제한시간을 관리한다.
- 조건이 바뀐 뒤 늦게 도착한 이전 응답이 최신 조건·선택 계획을 덮어쓰지 않도록 처리한다.
- 실행되지 않은 조회·저장·알림 발송을 완료했다고 설명하지 않는다.

## 8. AI·MainAgent·SubAgent·Skill의 역할

AI는 입력 구조화, 확인 질문, 계산 결과 설명을 담당한다. 시간 계산, 경로 유효성, 확인·선택 상태, 횟수 제한, 권한 검증은 테스트 가능한 코드로 처리한다.

- Hermes의 Solar Pro4: 사용자 대화, MCP 도구 호출, 질문·결과 전달.
- MCP 연결 계층: 도구 스키마 검증, 기존 HTTP API 연결, 구조화 결과·오류 전달. 계산 로직이나 Agent 체계를 중복 구현하지 않는다.
- 백엔드 MainAgent: 요청·현재 상태를 파악하고 SubAgent 배정, 실행 순서, 결과 취합, 사용자 확인 필요 상태를 조정한다.
- 백엔드 SubAgent: 역할에 따라 필요한 Skill을 선택한다. 하나의 SubAgent가 여러 Skill을 사용할 수 있고 매 요청에 모든 Skill을 실행하지 않는다.
- 코드 도구·도메인 로직: 교통 조회, 시간 계산, 경로 검증, 상태·권한 검증을 수행한다. 모든 함수를 별도 Agent로 만들지 않는다.

이 구조는 개발용 코딩 에이전트 분업과 별개다. Hermes의 MCP 도구 호출 자체가 서비스 내부 SubAgent·Skill 사용을 증명하지 않는다.

같은 입력을 Hermes와 서버가 불필요하게 반복 해석하지 않도록 interpret_trip의 구조화 결과를 후속 작업의 기준으로 사용한다. Agent 간 입력·출력 스키마와 호출 횟수·전체 제한시간·실패 규칙을 정하고 공개 API 제한시간과 일치시킨다. 모델 출력 형식이 잘못되면 검증 실패로 처리한다.

### 기존 Skill 자산

.hermes/skills는 Skill 원본의 단일 저장 위치다. 원본이 없는 폴더는 준비 상태이며 가짜 SKILL.md로 구현 완료를 표현하지 않는다. Skill 생성 지침과 테스트 대화는 실제 구현 파일·실행 근거와 구분한다.

Timely에서 동작한 Skill이 새 런타임에서도 그대로 동작한다고 가정하지 않는다. 필요한 도구·입출력·권한을 연결하고 재검증하며, 기존 Skill이 있다는 이유로 보류 기능을 활성화하지 않는다.

| Skill | 서비스에서 활용할 책임 | 이번 우선도 |
|---|---|---|
| resolve-trip-details | 이동 조건 해석·확정 | 핵심 |
| leave-by | 약속 출발시각 정책·설명 | 핵심 |
| maginot-line | 막차 마지노선 정책·설명 | 데이터 검증 우선 |
| plan-b-recovery | 재탐색 대안 비교·설명 | 핵심 |
| route-risk-checker | 계산된 여유·위험 설명 | 핵심 결과에 통합 |
| schedule-ready | 일정 준비 정보 확인 | 입력 흐름에 필요한 부분 |
| journey-check-in | 출발·도착 확인 해석 | 후속 상태 관리 확장 |
| departure-alert | 출발 알림 판단 | 후속 알림 확장 |
| calendar-conflict | 여러 일정의 연결 가능성 | 후속 확장 |
| taxi-hybrid | 최소 택시 구간 대안 | 후속 확장 |
| route-deviation | 이탈 후 재탐색 승인 | 수동 흐름의 확인 정책 참고 |
| replan-guard | 자동 재탐색 주기·횟수 정책 | 자동 재탐색 도입 시 적용 |

## 9. 두 사람의 분업

| 담당 | 책임 | 인수 기준 |
|---|---|---|
| 본인 — MCP·Hermes·시연 | 도구 5개, 입력 스키마·HTTP 연결, 결과·오류 전달, 조건 확인·후보 선택 흐름, Hermes 연결 설정, 설치 안내·시연 | Hermes에서 도구를 호출해 확인된 조건으로 결과를 받고 대안을 선택한다 |
| 친구 — 핵심 백엔드 | 기존 FastAPI, MainAgent·SubAgent·Skill 통합, 장소·교통·AI 연결, 시간 계산·재탐색, 서버 테스트·배포 | 검증된 계산 결과와 출처·지원 범위·실패 사유를 계약대로 반환한다 |
| 공동 | API·MCP 계약, 확인·문맥·선택 상태의 소유권, 실제 지원 범위, 통합 테스트, 제출 조건·영상 | 두 계층이 같은 조건·상태·오류 의미를 유지한다 |

본인은 단순 API 포장뿐 아니라 질문 → 조건 확인 → 계산 → 후보 선택이라는 이용 흐름을 책임진다. 친구는 교통 결과의 근거와 계산 정확성, 서비스 Agent 실행을 책임진다.

두 개발자 모두 Spec Kit으로 명세·계획·작업을 관리한다. 개발 공통 규칙과 현재 Spec의 제공 형태는 MCP로 통일한다. 과거 저장·상태·알림 요구는 비적용으로 구분하며, 두 사람이 서로 다른 제품 범위나 API를 구현하지 않도록 맞춘다.

기존 파일·사용자 작업은 보존한다. 문서 방향 변경을 이유로 mobile·cli 등 기존 폴더를 삭제하거나 인프라·API를 임의 변경하지 않는다.

## 10. 4일 실행 일정

아래 일정은 개발 착수 기준이다. 실제 남은 시간에 맞춰 조정하되 마지막 날은 제출 안정화에 사용한다.

| 날짜 | 본인 | 친구 | 공동 완료 목표 |
|---|---|---|---|
| Day 1 | stdio MCP 연결, get_capabilities 도구와 Hermes 호출 검증 | health/capabilities 점검, 발급된 키로 장소·교통·막차 데이터 검증 | 첫 2시간에 계약 합의, 첫 4~6시간에 지원 범위 확인, Hermes → MCP → FastAPI 응답 수신 |
| Day 2 | interpret_trip·search_places·plan_journey 연결, 조건 확인·결과 흐름 | 실제 데이터 기반 입력 해석·경로 검증·출발시각 계산 | 자연어 → 부족 정보 확인 → 사용자 동의 → 근거 있는 출발시각까지 연결 |
| Day 3 | replan_journey·선택 흐름, 오류·시간 초과, 시연 리허설 | 재탐색·시간 경계·지원 가능한 막차 구현, 배포 검증 | 대안 선택과 실패 처리까지 통합. 저녁부터 기능 추가 중단 |
| Day 4 | 다른 환경의 설치·Hermes 연결 재현, README·영상 | 치명적 오류 수정, 실행·배포·지원 범위 안내 확인 | 회귀 검증, 제출 파일·필수 설정 점검, 최종 제출 |

매일 최소 두 번 통합한다. fixture로 MCP 계층을 개발할 수 있지만 실제 데이터 경로와 구분한다. 마지막 날 처음 MCP 연결이나 서버 배포를 시도하지 않는다.

### 가장 먼저 통과시킬 연결 테스트

> Hermes에서 Solar Pro4가 get_capabilities를 실제 호출하고, 지금 MCP 서버를 통해 받은 FastAPI 응답을 설명한다.

확인할 증거는 Hermes의 도구 호출 기록, MCP→백엔드 요청, 실제 응답과 데모 여부다. 일반 채팅으로 “연결됐다”는 답변만 받는 것은 성공 기준이 아니다. 이 테스트는 배선 검증이며 실제 교통 서비스 완성의 증거는 아니다.

## 11. 첫날 확인할 미확정 사항

- 설치된 Hermes 버전, 실행 OS·환경, MCP stdio 실행 경로·작업 디렉터리·의존성
- Solar Pro4의 정확한 모델 ID·제공처·접근 권한과 실제 MCP 도구 호출
- 백엔드 Agent 런타임 AI 설정, 중복 모델 호출 방지·횟수·시간 제한
- MCP 도구별 JSON Schema·구조화 결과·업무 오류와 실행 오류의 매핑
- 사용자 확인과 입력 조건의 결부 방식, 대화별 문맥·선택 계획의 저장 위치와 수명
- 백엔드 시연 Base URL, 배포 접근 권한·호출 제한·필수 환경 변수
- 실제 장소·교통 API의 서비스명·엔드포인트·권한·이용 조건·호출 한도·갱신 주기
- 장소 검색·노선 연결·미래 시각 경로·막차 환승 검증의 지원 범위
- 실제 Buffer 정책 버전과 수치, 제공처별 정보 최신성 기준
- 주최 측의 MCP 제출 허용 형태, 원격 주소 요구, 모델·플랫폼 사용 증빙 조건

미확정 값은 지원 기능처럼 하드코딩하지 않는다. 막차 지원 부족이나 조회 실패를 LLM 추정으로 채우지 않는다.

## 12. 완료 기준과 제출물

완료 여부는 설명 문구나 화면 시안이 아니라 실제 동작과 재현 가능한 근거로 판단한다.

1. 다른 개발 환경에서 README대로 설치해 Hermes가 MCP 서버를 연결하고 도구를 발견·호출할 수 있다.
2. Solar Pro4의 실제 도구 호출로 get_capabilities의 백엔드 응답을 받을 수 있다.
3. 자연어 요청에서 모호한 장소·시간은 확인하고 명확한 조건은 유지한다.
4. 사용자 최종 확인 없이 계산하지 않고, 조건 변경 후 과거 확인을 재사용하지 않음을 검증한다.
5. 확인된 이동 데이터로 권장 출발시각·목표 도착·경로·계산 근거·출처·기준시각을 표시한다.
6. 현재 대화의 선택 계획을 재탐색에 전달하고, 사용자 선택 전에는 새 후보를 적용하지 않는다.
7. 데이터 없음·미지원·API 오류·시간 초과에서 거짓 경로 대신 사유와 다음 행동을 안내한다.
8. 도착 여유 중복 계산, 자정·운행일 경계, 사용자 고정 조건, 지각과 여유 부족 구분을 검증한다.
9. 막차를 제공한다면 지원 범위의 운행일·종착역·환승 연결을 검증한다. 미지원 상태를 명시한다.
10. MainAgent의 SubAgent 배정과 실제 Skill 사용을 테스트·실행 기록으로 확인한다. 하나의 SubAgent가 여러 Skill을 사용할 수 있는 구조와 코드 계산 원칙을 유지한다.
11. 설치·실행·Hermes 연결·백엔드 배포 방법, 필수 설정, 지원 범위, 알려진 제한을 문서화한다.
12. 시연 영상·설정 예제·로그·제출 파일에 실제 비밀키가 노출되지 않았는지 확인한다.

제출 패키지는 다음을 포함한다.

- 지금 MCP 서버 코드와 의존성·실행 진입점
- 기존 FastAPI 백엔드 및 실행에 필요한 Agent·Skill·계산 코드
- 실제 비밀값을 제외한 Hermes 연결 설정·환경 변수 예시
- 설치·실행·검증·문제 해결 README
- 재현 가능한 테스트 입력과 실제 실행 결과
- 시연 영상과 지원 범위·한계·데모 여부 설명
- 원격 URL 등 주최 측이 요구하는 추가 항목은 확인 후 반영

대표 시연은 약속 입력 → 부족한 조건 확인 → 사용자 최종 확인 → 출발시각·근거 표시 → 환승 실패 재탐색 → 후보 선택으로 구성한다. 막차는 검증된 데이터 범위의 별도 시나리오로 보여준다.

데모 데이터로 재현한 장면은 그 사실을 표시한다. 실시간 교통 조회, 자동 모니터링, 저장·알림·배포 또는 실행하지 않은 테스트를 완료한 것으로 소개하지 않는다.

## 13. Hermes에서 개발을 시작할 때

1. IDEA.md → API_SPEC.md → 담당 agent_specs 및 specs 순서로 읽고 현재 구현을 확인한다.
2. 이 문서의 MCP 제품 방향을 기준으로 본인과 친구의 작업 경계, MCP·REST 계약, 확인·상태 소유권을 합의한다.
3. 각자 Spec Kit 명세·계획·작업을 정리한다. REST 계약 변경은 공동 합의 후 API_SPEC.md와 예제 JSON에 함께 반영한다.
4. 가장 먼저 get_capabilities로 Hermes → MCP → FastAPI 연결을 검증한다.
5. 가장 작은 약속 이동 흐름을 종단 간 연결한 뒤, 검증된 범위에서 재탐색·막차로 확장한다.
6. 구현·실행·테스트 결과를 기록하고, 준비 폴더·fixture·실제 기능을 구분한다.

핵심 기준은 사용자가 자신의 조건에 맞는 **출발시각과 다음 행동을 이해하고 실제로 사용할 수 있는가**다.

## 참고 자료

이 문서는 단독으로 읽어도 제품 목적과 범위를 이해할 수 있도록 작성했다. 기존 문서와 충돌할 때 제품 방향은 이 문서를 따르되, 구현 계약은 공동 개정한다.

- API_SPEC.md: MCP 연결 계층이 사용하는 기존 REST/JSON 계산 계약. MCP 세부 스키마·확인 상태 연결 방식은 공동 합의 대상
- Docs/api/examples.json: 가상 역·가상 시간표를 사용하는 계약 fixture
- agent_specs 및 specs: 역할·구현 명세. 최신 제품 방향과 일치 여부 확인 필요
- .hermes/skills: 실제 Skill 원본의 단일 저장 위치
- Docs/jigeum_prd_v1.0.md: 전체 서비스 PRD
- Docs/jigeum_agent_skills_proposal_v0.1.md: Agent와 코드 기반 도메인 로직의 책임
- Docs/jigeum_4day_mvp_team_plan_v0.1.md: MCP 2인·4일 분업 계획. 이 문서 9절과 같은 담당·제출 기준
- Docs/timely_skill_submission_plan_v0.1.md: 최초 Skill 제출 계획
- Docs/timely_additional_skills_my_prompts_v0.1.md: 본인 담당 추가 Skill 생성 지침
- Docs/timely_additional_skills_friend_prompts_v0.1.md: 친구 담당 추가 Skill 생성 지침

기술 확인 자료:

- [Hermes MCP 공식 문서](https://hermes-agent.nousresearch.com/docs/user-guide/features/mcp): 로컬 stdio·원격 HTTP 연결과 설정
- [MCP 아키텍처](https://modelcontextprotocol.io/docs/learn/architecture): 호스트·클라이언트·서버와 도구의 역할
- [Spec Kit](https://github.com/github/spec-kit): 두 개발자의 명세·계획·작업 관리
- [Cloud Run 컨테이너 실행 조건](https://docs.cloud.google.com/run/docs/container-contract): 백엔드 HTTP 리스닝 주소·PORT와 컨테이너 요구 사항
