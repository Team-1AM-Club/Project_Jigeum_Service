# Project Jigeum Service: 개발 공통 규칙

- IDEA.md → API_SPEC.md → 해당 agent_specs 및 specs 순서로 읽는다.
- 최신 제품 방향은 IDEA.md를 따른다. 이전 Docs 제안은 맥락 자료이며 충돌 시 무조건 구현하지 않는다.
- 서비스는 MCP로 제공한다. 독립 사용자 클라이언트를 개발하지 않으며, 독립 클라이언트 형태를 선택하는 단계도 아니다.
- 본인은 MCP 도구·Hermes 연결·사용 흐름·시연, 친구는 FastAPI·서비스 Agent·교통 데이터·계산·배포 담당이다. Hermes + Solar Pro4를 개발과 시연에 사용한다.
- 양쪽 개발자는 Spec Kit을 활용한다. API 변경은 공동 합의하고 API_SPEC.md와 예제 JSON을 함께 갱신한다.
- 서비스 MainAgent는 SubAgent를 지휘한다. SubAgent 하나가 여러 Skill을 사용할 수 있다. 이것은 개발용 코딩 에이전트 분업과 별개다.
- `.hermes/skills`는 Skill 원본의 단일 저장 위치다. 원본이 없는 폴더는 준비 상태이며 가짜 SKILL.md로 구현 완료를 표현하지 않는다.
- 경로·시간 계산·상태·권한은 검증 가능한 코드로 처리한다. 실시간 정보·막차·경로를 모델이 지어내지 않는다.
- 계산 전에 사용자에게 해석된 조건을 확인받는다. 재탐색 결과도 사용자 선택 없이 기존 계획에 적용하지 않는다.
- 택시비 상한선과 택시 최적화는 MVP에서 제외한다.
- 현재 MCP 범위에는 UserData DB·회원가입·영구 이동 이력이 필수가 아니다. 향후 저장·인증을 추가할 때 계약·접근 권한을 먼저 합의한다. DB/API 비밀키를 클라이언트·문서·로그에 포함하지 않는다.
- 기존 파일과 사용자 작업을 보존한다. 요청 없는 삭제·강제 푸시·인프라 변경은 하지 않는다.
- 실행하지 않은 테스트·조회·저장·배포를 성공했다고 보고하지 않는다. 데모 데이터와 실제 데이터를 구분한다.
